<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="?attr/colorPrimaryDark"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/userView"
        android:layout_width="@dimen/nav_userView_width"
        android:layout_height="@dimen/nav_userView_height"
        android:layout_marginLeft="@dimen/nav_header_vertical_spacing"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:scaleType="centerCrop"
        android:src="@drawable/user_head_3"
        android:clickable="true"
        android:onClick="userHandler" />

    <ImageView
        android:id="@+id/logout"
        android:layout_width="@dimen/nav_icon_size"
        android:layout_height="@dimen/nav_icon_size"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:layout_alignParentRight="true"
        android:src="@drawable/logout"
        android:clickable="true"
        android:onClick="logout" />

    <TextView
        android:id="@+id/username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/userView"
        android:paddingBottom="@dimen/nav_header_vertical_spacing"
        android:layout_alignParentBottom="true"
        android:gravity="bottom"
        android:text="AndroidStudio"
        android:textAppearance="@style/TextAppearance.AppCompat.Large"/>

</RelativeLayout>